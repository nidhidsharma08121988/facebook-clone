describe('Acceptance Test Add Post Feature:', () => {
  beforeAll(()=>{
    cy.visit('http:/localhost:3000');
  })

  it('Posts should be visible when facebook page is loaded,'() => {
    
  })
  it('User should be able to create post and it should be visible on page', () => {});
  it('User should be able to delete post that the user created', () => {});
  it('User should be able to edit post and edited post should be visible on page', () => {});
});
